"use strict";
/* class empreendedor{
    nome: string
    idade: number
    empresa: string
    constructor(nome: string, idade: number, empresa: string){
        this.nome = nome
        this.idade = idade
        this.empresa = empresa
    }
  
}

class projetos extends empreendedor{
    plano_empresarial: string
    lucro_anual: number

    constructor(nome: string, idade: number, empresa: string, plano_empresarial: string, lucro_anual: number){
        super(nome, idade, empresa)

        this.plano_empresarial = plano_empresarial
        this.lucro_anual = lucro_anual
    }
}

const proejects = new projetos("Daniel", 18, "Walmart", "Ações do mercado que estão em ascensão", 2000000)
console.table(proejects)
//  */
// class Circulo{
//     raio: number = 30;
//     pi:number = 20;
//     calcularArea(pi:number, raio:number) {
//         this.pi = pi
//         this.raio = raio
//         // return (pi * (raio ** 2)) 
//     }
//     calcularPerimetro():number{
//             return (2 * this.pi * this.raio)
//         }
//     logDetails_Area(): void{
//             console.log(`${(this.pi * (this.raio ** 2))} `)
//         }
//     logDetails_perimetro(): void{
//             console.log(`${(this.pi * (this.raio ** 2))} `)
//         }
//     }
